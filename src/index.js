import React from 'react';
import ReactDOM from 'react-dom';
import App from './components';
import './index.css';
//rendering our main parent component App into the DOM
ReactDOM.render(
  <App />,
  document.getElementById('root')
);
